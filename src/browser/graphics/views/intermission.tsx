import { render } from '../../render';
import { ThemeProvider } from '../components/theme-provider';


const Intermission = () => {

  return (
    <ThemeProvider>
      <></>
    </ThemeProvider>
  );
};

render(<Intermission />);
