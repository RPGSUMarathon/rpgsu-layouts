import { render } from '../../render';
import { ThemeProvider } from '../components/theme-provider';


const Game = () => {

  return (
    <ThemeProvider>
      <></>
    </ThemeProvider>
  );
};

render(<Game />);
